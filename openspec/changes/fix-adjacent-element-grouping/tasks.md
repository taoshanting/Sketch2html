## 1. 优化 detectLeftRightGroups 方法

- [x] 1.1 在分组前先检测紧邻元素组（间距 ≤ 10px）
- [x] 1.2 将紧邻元素组作为整体进行左右分组
- [x] 1.3 确保紧邻元素不会被中心线分割

## 2. 修复 groupByRow 阈值比较逻辑

- [x] 2.1 修改 groupByRow 使用前一个元素的 Y 坐标比较，而不是行首元素
  - 问题：原逻辑使用 `currentRowY`（行首元素 Y），导致 Y 坐标逐渐增加的元素被分到不同行
  - 修复：改为与前一个元素 `prevY` 比较，确保相邻元素能正确分组

## 3. 测试验证

- [x] 3.1 验证 `编组 16备份 3` (8E73971D) 在 `机构MOB12-24` 后面 ✅
- [x] 3.2 验证它们在同一个 Row 中 ✅
- [x] 3.3 验证其他行分组不受影响 ✅
